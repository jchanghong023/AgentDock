# 项目文件存放规则

- 本项目运行过程中产生的所有临时文件和下载文件 MUST 仅存放在项目根目录的 `.tmp/` 中，可按用途建立子目录。
- 执行会产生临时文件或下载文件的命令、脚本和工具前，MUST 将其临时目录和下载输出目录配置到 `.tmp/`，不得将这些文件写入项目其他目录或系统临时目录。
- `.tmp/` MUST 被 Git 忽略，其内容 MUST NOT 提交到版本管理。
