# 运行验收清单（全部待实机执行）

本文件列出应执行的验收，不是已经通过的结果。实际本次检查见 verification。

## 基础构建

- [ ] cargo fmt / check / test / clippy 完成，保存工具链版本与 Cargo.lock。
- [ ] Windows 与 Linux 各自构建，不用一平台结果代替另一平台。
- [ ] --self-test 返回 DEVHUB_PTY_SELF_TEST_OK，确认真 PTY 双向通信。
- [ ] Xvfb GUI 冒烟返回事件循环标记，随后人工检查截图与输入。

## 产品行为

- [ ] 无菜单；左上工作目录/会话、左下系统树、右侧一个内容区。
- [ ] 目录星标可开关，置顶后重启仍保留；星标不创建 worktree。
- [ ] 新建两个真实会话，来回切换不重启进程。
- [ ] 单击文件不读取内容；双击才预览。
- [ ] 文件预览期间终端命令继续工作；关闭预览可返回原终端。
- [ ] 关闭运行中的终端/退出应用有确认；取消不会中断进程。
- [ ] 历史会话无 resume 时明确提示，不伪装恢复 Agent 聊天。

## 终端

- [ ] 系统 shell、用户实际 OMP、vim、less、交互式 Python 分别测试。
- [ ] Ctrl+C 中断、Ctrl+D EOF、Tab 补全、方向键、Home/End、功能键正确。
- [ ] Ctrl+Shift+C/V、Shift+Insert、右键粘贴、括号粘贴正确。
- [ ] 切换窗口/项目/文档后的焦点和快捷键不串到其他终端。
- [ ] 中文 IME 候选框位置、组合文本、提交不重复；高 DPI 下重复测试。
- [ ] 中文双宽字符、组合字符、Emoji 不错位；不可假定所有复杂字体都通过。
- [ ] alternate screen 切换和退出恢复正确；鼠标模式、Shift 强制选择正确。
- [ ] 单击/双击/三击选择、跨行复制、窗口 resize、回滚末端正确。
- [ ] 持续大输出、多个隐藏会话、旧 CPU、远程桌面分别测输入延迟和占用。

## 文件规模与异常

- [ ] 已有百万/千万总文件的机器上，启动不发生递归遍历。
- [ ] 单目录大量文件分成多页，内存不随总条目一次膨胀。
- [ ] 拒绝访问、断开的网络目录、无效路径可见报错，终端仍可操作。
- [ ] 折叠目录后，旧后台结果不能重新打开它。
- [ ] UTF-8/UTF-16 BOM、长行、二进制、超大 Markdown 的限制和提示正确。
- [ ] state.json 损坏不被静默覆盖；重复实例拒绝共用写状态目录。

## CentOS 7 专项

- [ ] 所有二进制/共享库 GLIBC 要求 <= 2.17，其他 ABI 符号也匹配。
- [ ] 实际 CentOS 7、目标 X11/远程桌面、无 GPU 环境运行。
- [ ] 真实测量首次启动、重复启动、中文输入、终端刷新延迟；不引用猜测数字。
